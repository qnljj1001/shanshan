# 2026-08-03 进度与文档修订说明

> 今日完整进展记录。涵盖：环境定型、git 仓库、代码、文档、git push。
> 后续接手开发时，先看这份文档即可了解项目当前状态。

---

## 1. 环境定型

| 项 | 选型 | 备注 |
|---|---|---|
| 操作系统 | Windows 11 | 不使用 WSL |
| IDE | PyCharm | Python Interpreter 已切到 conda 环境 `fastapi-demo` |
| Python 环境 | Conda `fastapi-demo`（Python 3.13） | 不是 `ai-doc-assistant` |
| Git 客户端 | Git for Windows（git bash） | 命令行备用 |
| 远端 | GitHub `https://github.com/qnljj1001/shanshan.git` | PAT 认证 |

---

## 2. 仓库与目录

仓库根：`D:/shanshan/ai-doc-assistant/`

```text
ai-doc-assistant/
├── .env.example                   # 配置模板（含 OpenAI / 后端 / DB / RAG 默认项）
├── .gitignore                     # 屏蔽 .env、IDE、私人文件、__pycache__、data/
├── README.md                      # 项目入口
├── backend/
│   ├── .env                       # 用户私有（git 忽略）
│   ├── .idea/                     # PyCharm 本地配置（git 忽略）
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                # FastAPI 入口
│   │   └── services/
│   │       ├── __init__.py
│   │       └── llm.py             # OpenAI 兼容 LLM 客户端
│   ├── requirements.txt
│   └── test_main.http             # PyCharm HTTP 测试（git 忽略）
└── docs/                          # 全部中文文件名
    ├── 2026-08-03-文档修订说明.md   # 本文档
    ├── API 设计.md
    ├── RAG 设计.md
    ├── RAG 评估文档.md
    ├── 产品需求文档.md
    ├── 开发指南.md
    ├── 数据模型.md
    ├── 第一阶段接口实现说明.md
    ├── 系统架构.md
    ├── 行动路径.md
    ├── 部署指南.md
    └── 面试材料.md
```

---

## 3. Git 历史与当前状态

3 个 commit（最新在前）：

| commit | 标题 | 内容 |
|---|---|---|
| 7e5edaa | 第一阶段第一次提交 | 后端代码（backend/app/main.py 等） |
| 802247c | 第一阶段第一次提交 | 代码与文档同步 |
| 96fa734 | 初始化 --v0.1 | 项目骨架 + 11 个文档 |

工作区当前状态（截至 2026-08-03 20:27）：

- 已跟踪：15 个文件（`.env.example`、`.gitignore`、`README.md`、5 个 backend 文件、9 个 docs）
- 未跟踪：`backend/.env`、`backend/.idea/`、`backend/app/__pycache__/`、`backend/test_main.http`
- 改动未提交：`backend/app/main.py`（添加了 `/` 路由和 `__main__` 启动块）—— **用户自行 commit**

---

## 4. 代码现状（用户自行完成）

### 4.1 `backend/app/main.py`

```python
from fastapi import FastAPI, HTTPException
from pydantic import BaseModel, Field
from app.services import llm

app = FastAPI(title="AI Doc Assistant")


class ChatRequest(BaseModel):
    message: str = Field(..., min_length=1)


class ChatResponse(BaseModel):
    answer: str


@app.get("/")
async def home():
    return {"status": "ok"}


@app.get("/api/health")
async def health():
    return {"status": "ok"}


@app.post("/api/chat", response_model=ChatResponse)
async def chat(request: ChatRequest):
    try:
        answer = llm.chat(request.message)
    except RuntimeError as exc:
        raise HTTPException(status_code=500, detail=str(exc)) from exc
    except Exception as exc:
        raise HTTPException(status_code=502, detail="LLM request failed") from exc
    return ChatResponse(answer=answer)


if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="127.0.0.1", port=8000)
```

接口清单：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/` | 简易存活检查，返回 `{"status": "ok"}` |
| GET | `/api/health` | 健康检查 |
| POST | `/api/chat` | 聊天，body `{message: str}`，返回 `{answer: str}` |

错误处理：LLM 抛 `RuntimeError` → 500，抛其他异常 → 502。

### 4.2 `backend/app/services/llm.py`

特性：

- 自动从 `backend/.env`（相对文件位置）加载环境变量
- 单例 OpenAI 客户端（懒加载）
- 自动给 `OPENAI_BASE_URL` 补 `/v1` 后缀
- 校验 `OPENAI_BASE_URL` / `OPENAI_API_KEY` 必须存在
- 默认 model 名 `gpt-4.1-mini`，可通过 `MODEL_NAME` 覆盖

### 4.3 `backend/requirements.txt`

```text
fastapi
uvicorn
openai
python-dotenv
pydantic
```

### 4.4 `.env.example`

含 4 大组：

- **OpenAI 兼容 API**：`OPENAI_BASE_URL` / `OPENAI_API_KEY` / `MODEL_NAME` / `EMBEDDING_MODEL_NAME`
- **Backend**：`BACKEND_HOST` / `BACKEND_PORT` / `ENV` / `LOG_LEVEL`
- **Database**：`DATABASE_URL`（默认 SQLite）
- **RAG**：`UPLOAD_DIR` / `VECTOR_INDEX_DIR` / `CHUNK_SIZE` / `CHUNK_OVERLAP` / `RETRIEVAL_TOP_K`

---

## 5. 文档适配（已完成）

5 个文档改为适配 PyCharm + conda `fastapi-demo`：

| 文件 | 改动点 |
|---|---|
| `README.md` | "快速启动"小节新增；"当前第一步"改 PyCharm；启动命令 `uvicorn app.main:app` |
| `docs/开发指南.md` | 第 1 节去掉 WSL、加 PyCharm 设置；第 2 节 conda 化；第 8 节后端启动用 conda |
| `docs/行动路径.md` | "今天"任务段改 PyCharm + conda |
| `docs/第一阶段接口实现说明.md` | 第 3 节 conda 化；第 5 节测试命令 conda 化 |
| `docs/部署指南.md` | 第 2 节 conda 化 |

---

## 6. Git Push 状态

| 通道 | 状态 |
|---|---|
| PyCharm `VCS → Git → Push` + PAT | ✅ 推成功 |
| `git bash` 手动 `git push origin main` | ⚠️ 失败（内置 terminal 无 TTY，无法交互输入 PAT） |
| sandbox 内 `git push` | ⚠️ 失败（沙箱无认证 + 无网络出站） |

**结论**：当前以 PyCharm 推送为准。`git bash` 推送失败不影响正常使用。

---

## 7. 今日完成 vs 行动路径对照

`docs/行动路径.md` "今天"段的目标是"创建项目基础结构，验证模型接口可用"。

| 任务 | 状态 | 备注 |
|---|---|---|
| 1. PyCharm 打开项目，Interpreter 选 fastapi-demo | ✅ | |
| 2. 初始化 Git 仓库 | ✅ | 仓库根在 `ai-doc-assistant/` |
| 3. 创建后端目录 | ✅ | 已迁移到 `backend/app/` 结构 |
| 4. 激活 conda 环境 | ✅ | `fastapi-demo` |
| 5. 安装依赖 | ✅ | `backend/requirements.txt` 已生成 |
| 6. 写 `.env` | ✅ | 用户自己填了 key，未跟踪 |
| 7. 最小模型调用脚本 | ✅ | 直接落到 `/api/chat` 接口（已超出原计划） |
| 8. 第一次 commit | ✅ | 3 个 commit |

**超额完成**：原"今天"目标只要求模型调用脚本，但实际上已经把"明天"任务里的 `/api/chat` 接口和 `services/llm.py` 也实现了。明天任务的 1-5 项已落地。

---

## 8. 下一阶段建议（用户自行选择）

按 `docs/行动路径.md` 的"明天"段，V0.1 后端聊天接口已完成（commit `16b2224`），剩下两件事：

- [x] **本地试运行一次**：`conda activate fastapi-demo && cd backend && uvicorn app.main:app --reload --port 8000`，浏览器访问 `http://127.0.0.1:8000/docs` 验证 ✅
- [x] **commit 工作区改动**：✅
- [ ] **打 tag**（V0.1 收尾可选动作）：
  ```bash
  git tag v0.1-api
  git push origin v0.1-api
  ```
- [ ] **进入第 3 天**（流式输出 `/api/chat/stream`）

V0.1 已闭环，可直接进入"第 3 天"。

---

## 9. 长期维护要点

- conda 环境名锁定 `fastapi-demo`，后续文档不要再用 `ai-doc-assistant` 当环境名。
- backend 启动命令一律 `uvicorn app.main:app`（入口在 `backend/app/main.py`）。
- 任何 backend 结构变更（再拆分 `app/api/`、`app/db/` 等），需要同步更新启动命令。
- IDE 配置（`.idea/`、`test_main.http`）由 `.gitignore` 屏蔽，不要提交。
- `.env` 永远不提交，key 安全优先。
- Git push 优先走 PyCharm，git bash 推不动的根因是内置 terminal 无 TTY，不是配置问题。